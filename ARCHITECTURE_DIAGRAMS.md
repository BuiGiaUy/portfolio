# Architecture Diagrams

## E2E Testing Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                         E2E Test Execution                        │
└──────────────────────────────────────────────────────────────────┘

1. AUTH FLOW (auth.spec.ts)
   ┌─────────────┐     POST /api/auth/login      ┌─────────────┐
   │  Playwright │ ───────────────────────────▶  │   Backend   │
   │   Browser   │ ◀───────────────────────────  │  (NestJS)   │
   └─────────────┘  Set HttpOnly cookies         └─────────────┘
         │                                              │
         │ Navigate to /dashboard                      │
         ▼                                              ▼
   ┌─────────────┐                              ┌─────────────┐
   │  Frontend   │ ──────── GET /api/auth/me  ─▶│   Verify    │
   │  (Next.js)  │ ◀──────── User data  ────────│  JWT Auth   │
   └─────────────┘                              └─────────────┘


2. PROTECTED ROUTE TEST (protected-routes.spec.ts)
   ┌─────────────┐                              ┌─────────────┐
   │  Playwright │ ─── Visit /dashboard ──────▶ │  Next.js    │
   │  (No Auth)  │                              │  Middleware │
   └─────────────┘                              └─────────────┘
         │                                              │
         │                                              │ No
         │                                              │ accessToken
         │                                              │ cookie
         │                                              ▼
         │                                      Redirect to
         │                                      /login?redirect=/dashboard
         │                                              │
         └───────────────────────────────────────────────┘


3. CRUD TEST (projects.spec.ts)
   ┌─────────────┐                              ┌─────────────┐
   │  Playwright │ ── POST /api/projects ─────▶ │   Backend   │
   │ (with auth) │                              │   +Prisma   │
   └─────────────┘                              └─────────────┘
         │                                              │
         │                                              │ Insert
         │                                              │ into DB
         │                                              ▼
         │                                      ┌─────────────┐
         │ ◀────── Project created ─────────── │  PostgreSQL │
         │                                      └─────────────┘
         │
         └──── Verify UI updated ────▶ Frontend re-fetches
                                        and displays new project
```

---

## Observability Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                     Backend Observability                         │
└──────────────────────────────────────────────────────────────────┘

REQUEST FLOW:
   HTTP Request
         │
         ▼
   ┌─────────────────────┐
   │  Rate Limiter       │ ← Redis check
   │  Middleware         │
   └─────────────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  Controller/        │
   │  Use Case           │ ──▶ StructuredLogger.log("User logged in")
   └─────────────────────┘          │
         │                           ▼
         │                    ┌─────────────┐
         │                    │   Console   │ ← JSON in prod
         │                    │   (stdout)  │ ← Human in dev
         │                    └─────────────┘
         ▼
   ┌─────────────────────┐
   │  Global Exception   │
   │  Filter             │
   └─────────────────────┘
         │
         ├──▶ StructuredLogger.error("DB failed", ...)
         │         │
         │         ▼
         │    ┌─────────────┐
         │    │  Log to     │
         │    │  stdout     │
         │    └─────────────┘
         │
         └──▶ if (status >= 500) → Sentry.captureException(error)
                   │
                   ▼
              ┌─────────────┐
              │   Sentry    │ ← Only 5xx errors
              │  Dashboard  │ ← User context (userId)
              └─────────────┘


┌──────────────────────────────────────────────────────────────────┐
│                     Frontend Observability                        │
└──────────────────────────────────────────────────────────────────┘

ERROR FLOW:
   User Action → Unhandled Error
         │
         ▼
   ┌─────────────────────┐
   │  Global Error       │
   │  Boundary           │ ──▶ useEffect(() => Sentry.captureException(error))
   │  (global-error.tsx) │
   └─────────────────────┘
         │
         ▼
   ┌─────────────────────┐
   │   Sentry Dashboard  │ ← Error details
   │                     │ ← Session replay
   │                     │ ← User context (userId)
   └─────────────────────┘
```

---

## Data Flow: Login with Observability

```
┌──────────────────────────────────────────────────────────────────┐
│            Complete Login Flow with Tracking                      │
└──────────────────────────────────────────────────────────────────┘

1. USER SUBMITS LOGIN FORM
   ┌─────────────┐
   │   Browser   │ ─── POST /api/auth/login ───▶
   │  (Next.js)  │     { email, password }
   └─────────────┘

2. BACKEND PROCESSES REQUEST
                              ┌─────────────────────┐
                              │  AuthController     │
                              │                     │
                              │  try {              │
                              │    const result =   │
                              │      loginUseCase   │
                              │        .execute()   │
                              └─────────────────────┘
                                       │
                                       ▼
                              ┌─────────────────────┐
                              │  LoginUseCase       │
                              │                     │
                              │  - Verify password  │
                              │  - Generate JWT     │
                              │  - Create refresh   │
                              └─────────────────────┘
                                       │
                                       ▼
                              ┌─────────────────────┐
                              │  Success!           │
                              │                     │
                              │  logger.log(        │
                              │    'User logged in',│
                              │    { userId }       │ ───▶ Console (JSON)
                              │  )                  │
                              │                     │
                              │  setSentryUser(     │
                              │    userId           │ ───▶ Sentry context
                              │  )                  │
                              └─────────────────────┘
                                       │
                                       ▼
3. RESPONSE WITH COOKIES
   ┌─────────────┐  ◀── Set-Cookie: accessToken=...
   │   Browser   │  ◀── Set-Cookie: refreshToken=...
   │  (Next.js)  │  ◀── { user, accessToken }
   └─────────────┘
         │
         ▼
   setSentryUser(user.id) ────────────────▶ Sentry (frontend context)


4. IF ERROR OCCURS
                              ┌─────────────────────┐
                              │  LoginUseCase       │
                              │                     │
                              │  throw new          │
                              │    UnauthorizedException
                              └─────────────────────┘
                                       │
                                       ▼
                              ┌─────────────────────┐
                              │ GlobalExceptionFilter│
                              │                     │
                              │  logger.error(...)  │ ───▶ Console (JSON)
                              │                     │
                              │  if (status >= 500) │
                              │    Sentry.capture   │ ───▶ Sentry dashboard
                              │                     │
                              │  return { statusCode,│
                              │           message }  │
                              └─────────────────────┘
                                       │
                                       ▼
   ┌─────────────┐  ◀── 401 Unauthorized
   │   Browser   │      { message: "Invalid credentials" }
   └─────────────┘
```

---

## Test Isolation & Cleanup

```
┌──────────────────────────────────────────────────────────────────┐
│                    Test Lifecycle                                 │
└──────────────────────────────────────────────────────────────────┘

test.describe('Auth Flow', () => {

  test.beforeEach(async ({ context }) => {
    // 1. CLEAN STATE
    await clearAllCookies(context);
    ───────────────────────────────────▶ ┌─────────────┐
                                          │   Browser   │
                                          │   Context   │
                                          │  (isolated) │
                                          └─────────────┘
  });

  test('login with valid credentials', async ({ page }) => {
    // 2. EXECUTE TEST
    await loginViaUI(page, TEST_USERS.user);
    ─────────────────────────────────▶ Real API call
                                        Real cookies set

    // 3. ASSERTIONS
    await assertAuthCookiesExist(page);
    ───────────────────────────────────▶ ┌─────────────┐
                                          │  accessToken│ ✓
                                          │  refreshToken│ ✓
                                          │  httpOnly=true│ ✓
                                          └─────────────┘
  });

  test.afterEach(async ({ context }) => {
    // 4. CLEANUP (automatic)
    // Playwright destroys context after each test
    // No state carried to next test
  });
});


ISOLATION GUARANTEES:
┌───────────┐  ┌───────────┐  ┌───────────┐
│  Test 1   │  │  Test 2   │  │  Test 3   │
│  Context  │  │  Context  │  │  Context  │
└───────────┘  └───────────┘  └───────────┘
     ↓              ↓              ↓
  Fresh          Fresh          Fresh
  state          state          state

Each test gets:
- New browser context
- No shared cookies
- Isolated storage
- Independent execution
```

---

## Sentry Error Filtering

```
┌──────────────────────────────────────────────────────────────────┐
│                    Error Decision Tree                            │
└──────────────────────────────────────────────────────────────────┘

                        Exception Occurs
                              │
                              ▼
                   ┌──────────────────────┐
                   │  GlobalExceptionFilter│
                   └──────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                ▼                           ▼
        Is HttpException?             Unknown Error
                │                           │
                ▼                           │
          Get status code                   │
                │                           │
        ┌───────┴────────┐                  │
        │                │                  │
        ▼                ▼                  ▼
    400-499          500-599           Treat as 500
        │                │                  │
        │                │                  │
        ▼                ▼                  ▼
   ┌─────────┐    ┌─────────┐        ┌─────────┐
   │  Log    │    │  Log    │        │  Log    │
   │  Only   │    │   +     │        │   +     │
   │         │    │ Sentry  │        │ Sentry  │
   └─────────┘    └─────────┘        └─────────┘
        │                │                  │
        ▼                ▼                  ▼
   Return 4xx     Return 5xx        Return 500
   to client      to client         to client


EXAMPLES:
┌────────────────────┬───────────┬────────────┬──────────────┐
│ Error Type         │ Status    │ Logged?    │ Sentry?      │
├────────────────────┼───────────┼────────────┼──────────────┤
│ ValidationError    │ 400       │ ✓          │ ✗            │
│ NotFoundError      │ 404       │ ✓          │ ✗            │
│ UnauthorizedError  │ 401       │ ✓          │ ✗            │
│ DatabaseError      │ 500       │ ✓          │ ✓            │
│ Unknown Exception  │ 500       │ ✓          │ ✓            │
│ Redis Timeout      │ 500       │ ✓          │ ✓            │
└────────────────────┴───────────┴────────────┴──────────────┘
```

---

## Log Sanitization Process

```
┌──────────────────────────────────────────────────────────────────┐
│                    PII Sanitization Flow                          │
└──────────────────────────────────────────────────────────────────┘

BEFORE SANITIZATION:
logger.log('User logged in', {
  userId: '123',
  email: 'john.doe@example.com',
  password: 'SecretPassword123',  ← DANGEROUS
  accessToken: 'eyJhbGc...',      ← DANGEROUS
  cookie: 'session=abc123...'     ← DANGEROUS
});

         │
         ▼
┌─────────────────────┐
│  sanitizeContext()  │
└─────────────────────┘
         │
         │ 1. Remove sensitive fields
         │    (password, token, cookie)
         │
         │ 2. Mask email
         │    john.doe@example.com → jo***@example.com
         │
         ▼
AFTER SANITIZATION:
{
  timestamp: '2025-12-26T02:00:00.000Z',
  level: 'info',
  message: 'User logged in',
  userId: '123',               ← SAFE (no PII)
  email: 'jo***@example.com'   ← MASKED
  // password: REMOVED
  // accessToken: REMOVED
  // cookie: REMOVED
}

         │
         ▼
┌─────────────────────┐
│  Console / Sentry   │ ← Only safe data
└─────────────────────┘
```

This implementation ensures production-grade security and observability! 🔒📊
